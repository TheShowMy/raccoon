{
  "name": "raccoon",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cargo run\" \"cd frontend && npm run dev\" --names \"backend,frontend\" --prefix-colors \"cyan,green\" --kill-others",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cargo build --release",
    "package": "npm run build && node scripts/package.js",
    "check": "cargo check && cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
